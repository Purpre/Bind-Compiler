Bind

A small experimental programming language and transpiler written in C++.

Bind is a programming language I am building from scratch as a learning
project. It currently transpiles Bind source code into C code, which is
then compiled using GCC.

The main goal of Bind is not to be practical or production-ready, but to
experiment with language design, lexing, code generation, control flow,
runtime concepts, and compiler architecture.
